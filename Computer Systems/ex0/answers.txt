1. The name of the file holding the private key is "rlogin.txt" and it is stored on the host computer.

2. The name of the file holding the public key is "rlogin.pub" and it is stored on the host computer,
    as well as the linux machine you ssh into. The path on the host machine is: c/Users/josep/.ssh/rlogin.
    On the linux machine the path is: /home/ugrads/nonmajors/.ssh/authorized_keys.

3. The output of echo ~cs3214/bin is: /home/courses/cs3214/bin.

4. After adding it to PATH and echo $PATH I get:
    /home/courses/cs3214/bin:/usr/share/Modules/bin:/usr/local/bin:/usr/bin:/usr/local/sbin:/usr/sbin:/var/lib/snapd/snap/bin:/home/ugrads/nonmajors/joearbo13/.local/bin:/home/ugrads/nonmajors/joearbo13/bin

5. 32 rlogin nodes that are named after types of trees are available this semester.

6. My $PS1 is: [joearbo13@pine ~]$

7. You can use the command "ls -al" to view all files in any directory.

8. While I can't actually find any documentation of this online, I assume it is to prevent rm from unintentionally deleting mor than the user wants to rm.

9. With the "diff" command the order of the files is specified by the order in which they are listed as input arguments.
    For example, "diff [options] File1 File2".

10. Using the "groups" command I found only one group that I am currently a part of called "Other". There are many more groups listed however
    so I am unsure if I should be added to more of them.

11. The file needs to be u(user) only and have no g(group), o(other) access, or a(all) access.