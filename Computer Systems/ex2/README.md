# Exercise 2

#### Link Checker
This exercise's purpose was to analyze the raw symbol table of a code base and parse it. After parsing, the program stores libraries, common symbols, functions, references, and prefixes. It is implemented in C++ (`linkchecker.cpp`) and in Python (`linkchecker.py`).